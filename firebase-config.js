// Fill this in with YOUR Firebase project's web config.
//
// Where to get it:
//   1. https://console.firebase.google.com → create (or open) a project
//   2. Build → Firestore Database → Create database (start in test mode is fine to begin with)
//   3. Project settings (gear icon) → General → "Your apps" → Add app → Web (</>)
//   4. Firebase shows you an object exactly like the one below — copy those
//      values in here.
//
// Until real values are pasted in below, the app runs in local-only mode:
// it still works, but nothing is shared between people and nothing survives
// a page refresh. See README.md for the full walkthrough.

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
